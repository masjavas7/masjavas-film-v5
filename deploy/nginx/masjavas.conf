upstream masjavas_backend {
  server 127.0.0.1:3000;
  keepalive 32;
}

server {
  listen 80;
  server_name film.example.com;

  client_max_body_size 12M;

  location / {
    proxy_pass http://masjavas_backend;
    proxy_http_version 1.1;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  location /health {
    proxy_pass http://masjavas_backend/health;
    access_log off;
  }
}

# After certbot:
# listen 443 ssl;
# ssl_certificate /etc/letsencrypt/live/film.example.com/fullchain.pem;
# ssl_certificate_key /etc/letsencrypt/live/film.example.com/privkey.pem;